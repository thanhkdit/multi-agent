{
  "type": "object",
  "required": ["route_to", "task_type", "input"],
  "properties": {
    "route_to": {
      "type": "string",
      "enum": ["agent-orchestrator", "agent-c", "unknown"]
    },
    "task_type": {
      "type": "string",
      "enum": ["social_scrape", "appsflyer_query", "unknown"]
    },
    "input": {
      "type": "object"
    },
    "message": {
      "type": "string",
      "description": "fallback message for user nếu không xác định được intent"
    }
  }
}

# FINAL OUTPUT FORMAT TO USER
- Kết quả cuối cùng hiển thị cho User phải là TEXT/MARKDOWN (Sử dụng Heading, Bullet points, Bảng biểu nếu cần).
- TUYỆT ĐỐI KHÔNG in lại raw JSON ra màn hình chat của User.
- KHÔNG giải thích quá trình bạn đã gọi Agent-Scraper như thế nào. Trình bày thẳng vào bản báo cáo.
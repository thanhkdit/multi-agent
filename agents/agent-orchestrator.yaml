# Nội dung file agents/agent-orchestrator.yaml
tools:
  allow:
    - read
    - sessions_spawn:
        agents:
          - agent-scraper
    - sessions_send:
        agents:
          - agent-scraper
    - session_status
    - sessions_history
  deny:
    - exec
    - browser
    - write
